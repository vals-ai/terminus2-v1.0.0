"""Metrics model for ATIF trajectories."""

from typing import Any

from model_library.base import QueryResultMetadata
from pydantic import BaseModel, Field, model_validator


class Metrics(BaseModel):
    """LLM operational and confidence data.

    Token fields align with QueryResultMetadata from model-library.
    """

    prompt_tokens: int | None = Field(
        default=None,
        description="Total input tokens, including cached and cache-write tokens",
    )
    completion_tokens: int | None = Field(
        default=None,
        description="Total output tokens generated by the LLM, including reasoning tokens",
    )
    cached_tokens: int | None = Field(
        default=None,
        description="Subset of prompt tokens that were cache hits",
    )
    reasoning_tokens: int | None = Field(default=None, exclude=True, repr=False)
    cache_read_tokens: int | None = Field(default=None, exclude=True, repr=False)
    cache_write_tokens: int | None = Field(default=None, exclude=True, repr=False)
    cost_usd: float | None = Field(
        default=None,
        description="Monetary cost of the API call in USD",
    )
    prompt_token_ids: list[int] | None = Field(
        default=None,
        description="Token IDs for prompt (input) tokens sent to the LLM, including chat history (if applicable)",
    )
    completion_token_ids: list[int] | None = Field(
        default=None,
        description="Token IDs for completion (response) tokens, enabling RL training without retokenization drift",
    )
    logprobs: list[float] | None = Field(
        default=None,
        description="Log probability assigned to each generated token",
    )
    extra: dict[str, Any] | None = Field(
        default=None,
        description="Other metrics",
    )

    model_config = {"extra": "forbid"}

    @model_validator(mode="after")
    def normalize_legacy_token_fields(self) -> "Metrics":
        """Accept the pre-v1.6 constructor fields while emitting v1.6 metrics."""
        if self.reasoning_tokens is None and self.cache_read_tokens is None and self.cache_write_tokens is None:
            return self

        if self.prompt_tokens is not None:
            self.prompt_tokens += (self.cache_read_tokens or 0) + (self.cache_write_tokens or 0)
        if self.completion_tokens is not None:
            self.completion_tokens += self.reasoning_tokens or 0
        if self.cached_tokens is None:
            self.cached_tokens = self.cache_read_tokens

        extra = dict(self.extra or {})
        if self.reasoning_tokens is not None:
            extra.setdefault("reasoning_tokens", self.reasoning_tokens)
        if self.cache_write_tokens is not None:
            extra.setdefault("cache_write_tokens", self.cache_write_tokens)
        self.extra = extra or None
        return self

    @classmethod
    def from_query_result_metadata(cls, metadata: QueryResultMetadata) -> "Metrics":
        """Normalize model-library token accounting to ATIF v1.6 fields."""
        extra = {
            key: value
            for key, value in {
                "reasoning_tokens": metadata.reasoning_tokens,
                "cache_write_tokens": metadata.cache_write_tokens,
            }.items()
            if value is not None
        }
        return cls(
            prompt_tokens=metadata.total_input_tokens,
            completion_tokens=metadata.total_output_tokens,
            cached_tokens=metadata.cache_read_tokens,
            cost_usd=metadata.cost.total if metadata.cost else None,
            extra=extra or None,
        )
